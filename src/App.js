import React from 'react';
import styled from '@emotion/styled';

export default () => <ExampleStyled>My App</ExampleStyled>;

const ExampleStyled = styled.div({
  fontSize: 40,
  color: '#38A',
  fontFamily: 'sans-serif',
});
